#include "monty.h"
/**
 *_nop - check the code.
 *@st: stack to be freed
 *@line_number: line number
 * Return: Always 0.
 */
void _nop(stack_t **st, unsigned int line_number)
{
	(void) st;
	(void) line_number;
}
